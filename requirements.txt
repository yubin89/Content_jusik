# 파이프라인 의존성
# 다음 단계에서 필요한 패키지는 해당 Step에서 추가합니다:
#   Step 5(Claude): anthropic
requests>=2.31
notion-client>=2.2
pykrx>=1.2.8        # Step 2: 한국거래소 수급 데이터 (KRX 회원 로그인 KRX_ID/KRX_PW 필요)
praw>=7.7           # Step 4: Reddit API 클라이언트
